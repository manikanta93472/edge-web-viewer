{
  "name": "edge-webviewer",
  "version": "1.0.0",
  "description": "Minimal TypeScript web viewer for processed frames (sample image, FPS, stats).",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.0.0"
  }
}
